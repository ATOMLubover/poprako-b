---
name: poprako-w-page-error
description: poprako-w 的错误页，用于处理页面不存在或路由级加载失败
tags:
  - 错误页
  - 兜底
  - 404
---

这是错误页。

## 页面定位

统一兜底页，用于：

1. 页面不存在。
2. 路由级渲染失败。
3. 路由加载异常。

## 用户可以做什么

1. 点击“返回首页”。

返回首页后会先到 `/`，再由 `/` 跳到 `/workspace`。

## 不要误说

1. 不要把它当成业务页。
2. 不要忽略未登录情况下，返回首页后仍可能继续跳去 `/login`。
